asyncapi: 3.0.0
info:
  title: Test AsyncAPI
  version: 1.0.0
channels:
  channel1:
    address: channel1
    messages:
      userSignedUp:
        $ref: '#/components/messages/message1'
operations:
  onReceive:
    action: receive
    channel:
      $ref: '#/channels/channel1'
  onSend:
    action: send
    channel:
      $ref: '#/channels/channel1'
components:
  messages:
    message1:
      payload:
        type: object
        properties:
          userId:
            type: string

