asyncapi: 3.0.0
info:
  title: Broken AsyncAPI
  version: 1.0.0
channels:
  userSignup:
    address: user/signup
operations:
  onUserSignup:
    action: invalid_action_type
    channel:
      $ref: '#/channels/userSignup'
