---
description: Skill trigger for changing what the api-processor build result contains.
applyTo: "src/**/*.ts"
---

When editing `src/**/*.ts` to change what a package version build result contains, apply the
`api-processor-authoring` skill.
